services:
  - type: web
    name: thumbnail-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python thumbnail_bot.py
    envVars:
      - key: API_ID
        sync: false
      - key: API_HASH
        sync: false
      - key: BOT_TOKEN
        sync: false
